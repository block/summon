---
description: The rectangular CTA system — small-radius rectangular buttons with uppercase mono labels, black fill primary on light planes, white/mint/dark-soft on midnight, one primary action per region. Reach when placing actions.
relates:
  - to: mono-labels
    as: reinforces
---

## Composition

**Rectangular CTA system + small-radius rectangles.** Actions are small-radius
rectangular buttons with uppercase mono labels — black fill (`--color-accent`) as
primary on light surfaces; white, mint, or dark-soft on midnight planes; outline
for secondary. Radius stays near 4px (`--radius-md`), with 8px (`--radius-lg`)
only for larger tab rails; circular geometry is reserved for isolated icon
controls. One primary action per region.

Primary actions stay small-radius rectangular with uppercase mono labels and
clear contrast, never full pills. Reach for border weight, fill polarity, or
placement to emphasize the chosen option before color. The mono labels on these
buttons follow the [mono label system](mono-labels); the buttons anchor the
claim-first regions on the [landing](landing), the conversion rows on
[pricing](pricing), the high-gravity actions on the midnight [proof](proof) plane,
and the single primary submit on the [workflow](workflow) form sheet.
