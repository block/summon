---
description: The dark elevated-plate and hairline depth system — presence built from brightness steps off the warm near-black canvas, 1px hairlines, and photographic atmosphere instead of drop shadows. Reach for it when layers must separate without breaking the flat, cinematic darkness.
relates:
  - to: cinematic-image
    as: reinforces
  - to: spec-system
    as: reinforces
---

## Composition

Hairlines and brightness create depth. Presence comes from photographic
atmosphere, 1px hairlines, dark-grey brightness steps, and occasional dark
gradients — never shadow stacks.

**Dark elevated plates.** Panels rise exactly one `--redline-brightness-step`
(`#303030`) above the canvas, separated by `--redline-hairline` rather than
shadow, with concise copy and images or large numerals as the focal point. Use
them for profile, driver, spec, and feature plates where a region must lift off
the stage without breaking the near-black mood.

**Editorial dark-to-body pacing.** Long pages move from a dark cinematic opening
into tighter editorial body sections, with large 96–128px breaks between acts, a
broad editorial container, and tighter 24–48px spacing inside cards and rows.
Depth is felt through this rhythm of brightness steps and breathing room.

**Shadow restraint.** Surface shadows stay inert; overlays make image text
legible, not decorative glass, and shadow tokens are reserved for transient
overlays — popovers and modals — only. Where regions meet, a single
`--redline-hairline` carries the separation rather than a stacked edge. Return to
the `--redline-canvas-warm` near-black for framing.

These plates frame the [cinematic image](cinematic-image) and host the
[large-number spec](spec-system) values; the only deliberate break in the
brightness-led calm is the [single red livery band](livery-band).
