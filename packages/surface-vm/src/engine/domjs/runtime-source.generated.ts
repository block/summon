// GENERATED FILE — do not edit by hand.
// Source of truth: src/engine/domjs/vm/{core,facade}.ts
// Regenerate: node scripts/build-vm-source.mjs   (checked by `pnpm test`)

export const DOMJS_CORE_MODULE_ID = "surface-vm:domjs-core";
export const DOMJS_FACADE_MODULE_ID = "surface-vm:domjs-facade";

export const DOMJS_CORE_SOURCE = "const __send = globalThis.__hostSend;\nlet nextNodeId = 0;\nlet nextHandlerId = 0;\nconst handlers = /* @__PURE__ */ new Map();\nlet mounted = false;\nlet patchQueue = [];\nfunction isMounted() {\n  return mounted;\n}\nfunction allocNodeId() {\n  nextNodeId += 1;\n  return \"snode:\" + nextNodeId;\n}\nfunction allocHandlerId() {\n  nextHandlerId += 1;\n  return \"shandler:\" + nextHandlerId;\n}\nlet activeEffect = null;\nconst reactiveDeps = /* @__PURE__ */ new WeakMap();\nconst MAX_EFFECT_RUNS_PER_FLUSH = 1e3;\nconst MAX_EFFECT_CASCADE_DEPTH = 32;\nconst CYCLE_ERROR_MESSAGE = \"domjs: reactive update cycle detected \\u2014 a binding writes state it also reads. Mutate state only in event handlers, not inside bindings or region render functions.\";\nlet effectRunDepth = 0;\nlet effectRunCount = 0;\nfunction depFor(target, key) {\n  let map = reactiveDeps.get(target);\n  if (!map) {\n    map = /* @__PURE__ */ new Map();\n    reactiveDeps.set(target, map);\n  }\n  let dep = map.get(key);\n  if (!dep) {\n    dep = /* @__PURE__ */ new Set();\n    map.set(key, dep);\n  }\n  return dep;\n}\nfunction bind(fn) {\n  const effect = {\n    run() {\n      const prev = activeEffect;\n      activeEffect = effect;\n      try {\n        return fn();\n      } finally {\n        activeEffect = prev;\n      }\n    }\n  };\n  effect.run();\n  return effect;\n}\nfunction notify(target, key) {\n  const map = reactiveDeps.get(target);\n  const dep = map && map.get(key);\n  if (!dep) return;\n  if (effectRunDepth === 0) effectRunCount = 0;\n  effectRunDepth += 1;\n  try {\n    if (effectRunDepth > MAX_EFFECT_CASCADE_DEPTH) {\n      throw new Error(CYCLE_ERROR_MESSAGE);\n    }\n    for (const effect of Array.from(dep)) {\n      if (effect === activeEffect) continue;\n      effectRunCount += 1;\n      if (effectRunCount > MAX_EFFECT_RUNS_PER_FLUSH) {\n        throw new Error(CYCLE_ERROR_MESSAGE);\n      }\n      effect.run();\n    }\n  } finally {\n    effectRunDepth -= 1;\n  }\n}\nconst proxyCache = /* @__PURE__ */ new WeakMap();\nconst rawOf = /* @__PURE__ */ new WeakMap();\nconst MUTATING_ARRAY_METHODS = [\"push\", \"pop\", \"shift\", \"unshift\", \"splice\", \"sort\", \"reverse\", \"fill\", \"copyWithin\"];\nfunction notifyAll(target) {\n  const map = reactiveDeps.get(target);\n  if (!map) return;\n  for (const key of Array.from(map.keys())) notify(target, key);\n}\nfunction reactive(obj) {\n  if (!obj || typeof obj !== \"object\") return obj;\n  const target = obj;\n  if (rawOf.has(target)) return target;\n  const cached = proxyCache.get(target);\n  if (cached) return cached;\n  const proxy = new Proxy(target, {\n    get(t, key, recv) {\n      if (typeof key === \"string\") {\n        if (Array.isArray(t) && MUTATING_ARRAY_METHODS.includes(key)) {\n          const method = Reflect.get(t, key);\n          return function(...args) {\n            const result = method.apply(t, args);\n            notifyAll(t);\n            return result;\n          };\n        }\n        if (activeEffect) depFor(t, key).add(activeEffect);\n      }\n      const value = Reflect.get(t, key, recv);\n      if (value && typeof value === \"object\" && typeof key === \"string\") {\n        return reactive(value);\n      }\n      return value;\n    },\n    set(t, key, value, recv) {\n      const raw = value && typeof value === \"object\" ? rawOf.get(value) ?? value : value;\n      const old = t[key];\n      const ok = Reflect.set(t, key, raw, recv);\n      if (ok && old !== raw && typeof key === \"string\") {\n        notify(t, key);\n        if (Array.isArray(t) && key !== \"length\") notify(t, \"length\");\n      }\n      return ok;\n    },\n    deleteProperty(t, key) {\n      const ok = Reflect.deleteProperty(t, key);\n      if (ok && typeof key === \"string\") notify(t, key);\n      return ok;\n    }\n  });\n  proxyCache.set(target, proxy);\n  rawOf.set(proxy, target);\n  return proxy;\n}\nfunction registerHandler(id, fn) {\n  handlers.set(id, fn);\n}\nfunction clearHandler(id) {\n  handlers.delete(id);\n}\nfunction emit(message) {\n  __send(JSON.stringify(message));\n}\nfunction output(payload) {\n  emit({ type: \"output\", payload });\n}\nfunction enqueuePatch(patch) {\n  if (!mounted) return;\n  patchQueue.push(patch);\n}\nconst pendingChildReplaces = /* @__PURE__ */ new Map();\nfunction scheduleChildrenReplace(node) {\n  if (!mounted) return;\n  pendingChildReplaces.set(node.__id, node);\n}\nfunction flushPatches() {\n  if (pendingChildReplaces.size > 0) {\n    for (const node of pendingChildReplaces.values()) {\n      patchQueue.push({\n        type: \"replace-region\",\n        regionId: node.__id,\n        children: node.__childNodes.map(serialize)\n      });\n    }\n    pendingChildReplaces.clear();\n  }\n  if (patchQueue.length === 0) return;\n  const patches = patchQueue;\n  patchQueue = [];\n  emit({ type: \"patch\", patches });\n}\nfunction serialize(node) {\n  if (node == null) return null;\n  if (node.__kind === \"fragment\") {\n    return { kind: \"fragment\", children: node.__childNodes.map(serialize) };\n  }\n  if (node.__kind === \"text\") {\n    node.__live = true;\n    return { kind: \"text\", id: node.__id, text: String(node.text) };\n  }\n  if (node.__kind === \"region\") {\n    const children = typeof node.__serializeChildren === \"function\" ? node.__serializeChildren() : node.renderChildren().map(serialize);\n    return { kind: \"region\", id: node.__id, children };\n  }\n  node.__live = true;\n  const out = {\n    kind: \"element\",\n    id: node.__id,\n    tag: node.tag,\n    attrs: Object.assign({}, node.attrs),\n    events: {},\n    children: node.__childNodes.map(serialize)\n  };\n  if (node.namespace) out.namespace = node.namespace;\n  for (const [type, handlerId] of node.events) out.events[type] = handlerId;\n  return out;\n}\nfunction mount(rootNode) {\n  if (rootNode == null) {\n    throw new Error(\"domjs surface must export a node as its default export.\");\n  }\n  mounted = true;\n  emit({ type: \"ready\" });\n  emit({ type: \"render\", tree: serialize(rootNode) });\n}\nconst __bridge = globalThis.__hostBridge;\nlet currentState = {};\nconst stateListeners = /* @__PURE__ */ new Set();\nfunction callTool(tool, args) {\n  if (!__bridge) return Promise.reject(new Error(\"host bridge unavailable\"));\n  return __bridge(String(tool), JSON.stringify(args || {})).then(\n    (json) => json ? JSON.parse(json) : null\n  );\n}\nfunction getState() {\n  return currentState;\n}\nfunction onState(cb) {\n  if (typeof cb !== \"function\") return function() {\n  };\n  stateListeners.add(cb);\n  cb(currentState);\n  return function() {\n    stateListeners.delete(cb);\n  };\n}\nglobalThis.__dispatch = async function(message) {\n  if (message.type === \"event\") {\n    const fn = handlers.get(message.payload.handlerId);\n    if (fn) {\n      try {\n        await fn(message.payload.event);\n      } catch (e) {\n        emit({ type: \"error\", error: String(e && e.message ? e.message : e) });\n      }\n    }\n    flushPatches();\n    return;\n  }\n  if (message.type === \"state\") {\n    currentState = message.state || {};\n    for (const cb of stateListeners) {\n      try {\n        cb(currentState);\n      } catch (e) {\n      }\n    }\n    flushPatches();\n    return;\n  }\n  if (message.type === \"destroy\") {\n    handlers.clear();\n    stateListeners.clear();\n    return;\n  }\n};\nexport {\n  allocHandlerId,\n  allocNodeId,\n  bind,\n  callTool,\n  clearHandler,\n  emit,\n  enqueuePatch,\n  getState,\n  isMounted,\n  mount,\n  onState,\n  output,\n  reactive,\n  registerHandler,\n  scheduleChildrenReplace,\n  serialize\n};\n";

export const DOMJS_FACADE_SOURCE = "import {\n  allocNodeId,\n  allocHandlerId,\n  registerHandler,\n  clearHandler,\n  enqueuePatch,\n  scheduleChildrenReplace,\n  serialize,\n  bind,\n  reactive\n} from \"surface-vm:domjs-core\";\nconst SVG_NS = \"http://www.w3.org/2000/svg\";\nfunction unsupported(name, hint) {\n  return function() {\n    throw new Error(\"domjs: \" + name + \" is not supported. \" + hint);\n  };\n}\nfunction toNode(value) {\n  if (value == null) return null;\n  if (typeof value === \"string\" || typeof value === \"number\") return new TextNode(value);\n  return value;\n}\nfunction walk(node, visit) {\n  const found = visit(node);\n  if (found) return found;\n  const children = node && node.__childNodes;\n  if (!children) return null;\n  for (const child of children) {\n    const childFound = walk(child, visit);\n    if (childFound) return childFound;\n  }\n  return null;\n}\nfunction collect(node, visit, out) {\n  if (visit(node)) out.push(node);\n  const children = node && node.__childNodes;\n  if (!children) return;\n  for (const child of children) collect(child, visit, out);\n}\nfunction elementChildren(node) {\n  return node && node.__childNodes || [];\n}\nfunction queryWithin(node, selector, includeSelf) {\n  const normalized = String(selector || \"\").trim();\n  const matcher = selectorMatcher(normalized);\n  if (includeSelf) {\n    const found = walk(node, function(candidate) {\n      return matcher(candidate) ? candidate : null;\n    });\n    if (found) return found;\n  } else {\n    for (const child of elementChildren(node)) {\n      const found = walk(child, function(candidate) {\n        return matcher(candidate) ? candidate : null;\n      });\n      if (found) return found;\n    }\n  }\n  return null;\n}\nfunction queryAllWithin(node, selector, includeSelf) {\n  const normalized = String(selector || \"\").trim();\n  const matcher = selectorMatcher(normalized);\n  const out = [];\n  if (includeSelf) collect(node, matcher, out);\n  else for (const child of elementChildren(node)) collect(child, matcher, out);\n  return out;\n}\nfunction selectorMatcher(selector) {\n  if (!selector) throw new Error(\"domjs: querySelector requires a selector.\");\n  if (selector.includes(\" \") || selector.includes(\">\") || selector.includes(\"+\") || selector.includes(\"~\") || selector.includes(\",\")) {\n    throw new Error('domjs: querySelector supports only simple selectors (#id, .class, tag, [data-ref=\"x\"], [data-role=\"x\"]).');\n  }\n  if (selector[0] === \"#\") {\n    const id = selector.slice(1);\n    if (!id) throw new Error(\"domjs: querySelector requires a non-empty id selector.\");\n    return function(node) {\n      return isElement(node) && node.id === id;\n    };\n  }\n  if (selector[0] === \".\") {\n    const name = selector.slice(1);\n    if (!name) throw new Error(\"domjs: querySelector requires a non-empty class selector.\");\n    return function(node) {\n      return isElement(node) && String(node.attrs[\"class\"] || \"\").split(/\\s+/).includes(name);\n    };\n  }\n  if (selector[0] === \"[\") {\n    const m = selector.match(/^\\[([A-Za-z_:][A-Za-z0-9:_.-]*)(?:=(?:\"([^\"]*)\"|'([^']*)'|([^\\]]+)))?\\]$/);\n    if (!m) throw new Error(\"domjs: querySelector supports only simple attribute selectors.\");\n    const attr = m[1].toLowerCase();\n    if (!attr.startsWith(\"data-\")) throw new Error(\"domjs: querySelector attribute selectors are limited to data-* attributes.\");\n    const expected = m[2] ?? m[3] ?? (m[4] ? m[4].trim() : void 0);\n    return function(node) {\n      if (!isElement(node)) return false;\n      const actual = node.attrs[attr];\n      if (expected === void 0) return actual !== void 0;\n      return String(actual) === expected;\n    };\n  }\n  if (!/^[A-Za-z][A-Za-z0-9-]*$/.test(selector)) throw new Error(\"domjs: querySelector supports only simple selectors.\");\n  const tag = selector.toLowerCase();\n  return function(node) {\n    return isElement(node) && node.tag === tag;\n  };\n}\nfunction isElement(node) {\n  return node && node.__kind === \"element\";\n}\nfunction camelToKebab(name) {\n  return name.replace(/[A-Z]/g, (c) => \"-\" + c.toLowerCase());\n}\nconst ON_EVENT_PROPS = [\n  \"click\",\n  \"dblclick\",\n  \"input\",\n  \"change\",\n  \"submit\",\n  \"keydown\",\n  \"keyup\",\n  \"keypress\",\n  \"focus\",\n  \"blur\",\n  \"mousedown\",\n  \"mouseup\",\n  \"mouseenter\",\n  \"mouseleave\",\n  \"mouseover\",\n  \"mouseout\",\n  \"pointerdown\",\n  \"pointerup\"\n];\nconst REFLECTED_PROPS = [\n  \"value\",\n  \"checked\",\n  \"disabled\",\n  \"hidden\",\n  \"placeholder\",\n  \"href\",\n  \"src\",\n  \"alt\",\n  \"title\",\n  \"type\",\n  \"name\",\n  \"min\",\n  \"max\",\n  \"step\"\n];\nfunction defineUnsupportedGetter(obj, name, hint) {\n  Object.defineProperty(obj, name, {\n    configurable: true,\n    get() {\n      throw new Error(\"domjs: \" + name + \" is not supported. \" + hint);\n    },\n    set() {\n      throw new Error(\"domjs: \" + name + \" is not supported. \" + hint);\n    }\n  });\n}\nclass TextNode {\n  __kind;\n  __id;\n  __live;\n  _text;\n  constructor(text) {\n    this.__kind = \"text\";\n    this.__id = allocNodeId();\n    this._text = \"\";\n    this.__live = false;\n    this.__setText(text);\n  }\n  // Internal: accept a string OR a function (reactive binding). A function is\n  // run as a tracked effect; when the reactive state it reads changes, the\n  // effect re-runs and emits a single set-text patch. This fine-grained model: only this text node updates, no region teardown.\n  __setText(value) {\n    if (typeof value === \"function\") {\n      const self = this;\n      bind(function() {\n        self.__apply(value());\n      });\n    } else {\n      this.__apply(value);\n    }\n  }\n  __apply(value) {\n    this._text = value == null ? \"\" : String(value);\n    if (this.__live) enqueuePatch({ type: \"set-text\", nodeId: this.__id, text: this._text });\n  }\n  get textContent() {\n    return this._text;\n  }\n  set textContent(value) {\n    this.__setText(value);\n  }\n  get text() {\n    return this._text;\n  }\n}\nclass ElementNode {\n  __kind;\n  __id;\n  tag;\n  namespace;\n  attrs;\n  events;\n  _handlers;\n  __childNodes;\n  __live;\n  __styleProxy;\n  __classList;\n  constructor(tag, namespace) {\n    this.__kind = \"element\";\n    this.__id = allocNodeId();\n    this.tag = String(tag).toLowerCase();\n    this.namespace = namespace;\n    this.attrs = {};\n    this.events = /* @__PURE__ */ new Map();\n    this._handlers = /* @__PURE__ */ new Map();\n    this.__childNodes = [];\n    this.__live = false;\n    this.__styleProxy = null;\n    this.__classList = null;\n    defineUnsupportedGetter(this, \"innerHTML\", \"Build nodes with document.createElement/createTextNode and append.\");\n    defineUnsupportedGetter(this, \"outerHTML\", \"Build nodes with document.createElement/createTextNode and append.\");\n    defineUnsupportedGetter(this, \"parentNode\", \"Hold references to nodes you created; no live-tree traversal.\");\n    defineUnsupportedGetter(this, \"parentElement\", \"Hold references to nodes you created; no live-tree traversal.\");\n    for (const type of ON_EVENT_PROPS) {\n      const self = this;\n      Object.defineProperty(this, \"on\" + type, {\n        configurable: true,\n        get() {\n          return null;\n        },\n        set(fn) {\n          self.removeEventListener(type);\n          if (typeof fn === \"function\") self.addEventListener(type, fn);\n        }\n      });\n    }\n    for (const prop of REFLECTED_PROPS) {\n      const self = this;\n      Object.defineProperty(this, prop, {\n        configurable: true,\n        get() {\n          const v = self.attrs[prop];\n          if (prop === \"checked\" || prop === \"disabled\" || prop === \"hidden\") return v === true || v === \"true\" || v === \"\";\n          return v === void 0 ? \"\" : v;\n        },\n        set(value) {\n          if (value === false || value == null) self.removeAttribute(prop);\n          else self.setAttribute(prop, value);\n        }\n      });\n    }\n  }\n  // el.style.backgroundColor = '...' — a write-through style object serialized\n  // to the style attribute (set-attribute op). Read-back returns only what was\n  // set through it; there are no computed styles in the VM.\n  get style() {\n    if (this.__styleProxy) return this.__styleProxy;\n    const self = this;\n    const declared = {};\n    const sync = () => {\n      const css = Object.entries(declared).filter(([, v]) => v !== \"\").map(([k, v]) => k + \": \" + v).join(\"; \");\n      if (css) self.setAttribute(\"style\", css);\n      else self.removeAttribute(\"style\");\n    };\n    this.__styleProxy = new Proxy(declared, {\n      get(t, key) {\n        if (key === \"setProperty\") return (name, value) => {\n          t[name] = String(value);\n          sync();\n        };\n        if (key === \"removeProperty\") return (name) => {\n          delete t[name];\n          sync();\n        };\n        if (typeof key === \"string\") return t[camelToKebab(key)] ?? \"\";\n        return void 0;\n      },\n      set(t, key, value) {\n        if (typeof key === \"string\") {\n          t[camelToKebab(key)] = value == null ? \"\" : String(value);\n          sync();\n        }\n        return true;\n      }\n    });\n    return this.__styleProxy;\n  }\n  set style(value) {\n    if (typeof value === \"string\") this.setAttribute(\"style\", value);\n  }\n  get classList() {\n    if (this.__classList) return this.__classList;\n    const self = this;\n    const classes = () => String(self.attrs[\"class\"] || \"\").split(/\\s+/).filter(Boolean);\n    const write = (list) => {\n      if (list.length) self.setAttribute(\"class\", list.join(\" \"));\n      else self.removeAttribute(\"class\");\n    };\n    this.__classList = {\n      add(...names) {\n        const list = classes();\n        for (const n of names) if (!list.includes(n)) list.push(n);\n        write(list);\n      },\n      remove(...names) {\n        write(classes().filter((c) => !names.includes(c)));\n      },\n      toggle(name, force) {\n        const has = classes().includes(name);\n        const next = force === void 0 ? !has : force;\n        if (next && !has) this.add(name);\n        if (!next && has) this.remove(name);\n        return next;\n      },\n      contains(name) {\n        return classes().includes(name);\n      }\n    };\n    return this.__classList;\n  }\n  setAttribute(name, value) {\n    if (typeof value === \"function\") {\n      const self = this;\n      bind(function() {\n        self.__applyAttr(name, value());\n      });\n      return;\n    }\n    this.__applyAttr(name, value);\n  }\n  __applyAttr(name, value) {\n    const v = value === true ? true : value === false ? false : String(value);\n    this.attrs[name] = v;\n    if (this.__live) enqueuePatch({ type: \"set-attribute\", nodeId: this.__id, name, value: v });\n  }\n  removeAttribute(name) {\n    delete this.attrs[name];\n    if (this.__live) enqueuePatch({ type: \"remove-attribute\", nodeId: this.__id, name });\n  }\n  get className() {\n    return this.attrs[\"class\"] || \"\";\n  }\n  set className(value) {\n    this.setAttribute(\"class\", value);\n  }\n  get id() {\n    return this.attrs[\"id\"] || \"\";\n  }\n  set id(value) {\n    this.setAttribute(\"id\", value);\n  }\n  get textContent() {\n    return this.__childNodes.map((c) => c.__kind === \"text\" ? c.text : \"\").join(\"\");\n  }\n  set textContent(value) {\n    this.__childNodes = [new TextNode(value)];\n    this.__structuralChange();\n  }\n  // Post-mount structural mutations coalesce into one replace-region patch per\n  // element per flush (an \"implicit region\": regionId = element id).\n  __structuralChange() {\n    if (this.__live) scheduleChildrenReplace(this);\n  }\n  append(...nodes) {\n    for (const node of nodes) {\n      const child = toNode(node);\n      if (child == null) continue;\n      this.__childNodes.push(child);\n    }\n    this.__structuralChange();\n    return this;\n  }\n  appendChild(node) {\n    this.append(node);\n    return node;\n  }\n  prepend(...nodes) {\n    const prepared = nodes.map(toNode).filter((n) => n != null);\n    this.__childNodes.unshift(...prepared);\n    this.__structuralChange();\n    return this;\n  }\n  insertBefore(node, reference) {\n    const child = toNode(node);\n    if (child == null) return node;\n    const index = reference == null ? -1 : this.__childNodes.indexOf(reference);\n    if (index === -1) this.__childNodes.push(child);\n    else this.__childNodes.splice(index, 0, child);\n    this.__structuralChange();\n    return node;\n  }\n  removeChild(node) {\n    const index = this.__childNodes.indexOf(node);\n    if (index === -1) throw new Error(\"domjs: removeChild: the node is not a child of this element.\");\n    this.__childNodes.splice(index, 1);\n    this.__structuralChange();\n    return node;\n  }\n  replaceChildren(...nodes) {\n    this.__childNodes = nodes.map(toNode).filter((n) => n != null);\n    this.__structuralChange();\n  }\n  remove() {\n    throw new Error(\"domjs: node.remove() is not supported. Call removeChild on the parent you created, or use region(...)/replaceChildren.\");\n  }\n  addEventListener(type, fn) {\n    if (typeof fn !== \"function\") throw new Error(\"domjs: addEventListener requires a function.\");\n    const handlerId = allocHandlerId();\n    this.events.set(type, handlerId);\n    this._handlers.set(type, handlerId);\n    registerHandler(handlerId, fn);\n    if (this.__live) enqueuePatch({ type: \"set-event-binding\", nodeId: this.__id, eventType: type, handlerId });\n  }\n  removeEventListener(type) {\n    const handlerId = this.events.get(type);\n    if (handlerId) {\n      clearHandler(handlerId);\n      this.events.delete(type);\n    }\n    if (this.__live) enqueuePatch({ type: \"clear-event-binding\", nodeId: this.__id, eventType: type });\n  }\n  querySelector(selector) {\n    if (!documentRoot) throw new Error(\"domjs: querySelector is not supported until a Surface Document template root is installed. Hold references to nodes you created.\");\n    return queryWithin(this, selector, false);\n  }\n  querySelectorAll(selector) {\n    if (!documentRoot) throw new Error(\"domjs: querySelectorAll is not supported until a Surface Document template root is installed. Hold references to nodes you created.\");\n    return queryAllWithin(this, selector, false);\n  }\n}\nclass FragmentNode {\n  __kind;\n  __childNodes;\n  constructor() {\n    this.__kind = \"fragment\";\n    this.__childNodes = [];\n  }\n  append(...nodes) {\n    for (const node of nodes) {\n      const child = toNode(node);\n      if (child != null) this.__childNodes.push(child);\n    }\n    return this;\n  }\n  appendChild(node) {\n    this.append(node);\n    return node;\n  }\n  querySelector(selector) {\n    if (!documentRoot) throw new Error(\"domjs: querySelector is not supported until a Surface Document template root is installed. Hold references to nodes you created.\");\n    return queryWithin(this, selector, false);\n  }\n  querySelectorAll(selector) {\n    if (!documentRoot) throw new Error(\"domjs: querySelectorAll is not supported until a Surface Document template root is installed. Hold references to nodes you created.\");\n    return queryAllWithin(this, selector, false);\n  }\n}\nclass RegionNode {\n  __kind;\n  __id;\n  _render;\n  __effect;\n  constructor(renderFn) {\n    this.__kind = \"region\";\n    this.__id = allocNodeId();\n    this._render = renderFn;\n    this.__effect = null;\n  }\n  renderChildren() {\n    const result = this._render();\n    const list = Array.isArray(result) ? result : result == null ? [] : [result];\n    return list.filter((n) => n != null);\n  }\n  // Auto-tracking: the FIRST render runs inside a tracked effect, so any\n  // reactive state the render fn reads will, on change, re-run the effect and\n  // emit a replace-region patch — no manual update() needed. The initial\n  // (build-phase) run produces no patch because enqueuePatch is silent then.\n  __serializeChildren() {\n    const self = this;\n    let children = [];\n    if (this.__effect) {\n      children = this.renderChildren().map(serialize);\n    } else {\n      this.__effect = bind(function() {\n        children = self.renderChildren().map(serialize);\n        if (self.__effect) {\n          enqueuePatch({ type: \"replace-region\", regionId: self.__id, children });\n        }\n      });\n    }\n    return children;\n  }\n  // Manual escape hatch (still supported, e.g. for non-reactive data sources).\n  update() {\n    enqueuePatch({\n      type: \"replace-region\",\n      regionId: this.__id,\n      children: this.renderChildren().map(serialize)\n    });\n  }\n}\nlet documentRoot = null;\nconst document = {\n  createElement(tag) {\n    return new ElementNode(tag);\n  },\n  createElementNS(ns, tag) {\n    return new ElementNode(tag, ns === SVG_NS ? \"svg\" : void 0);\n  },\n  createTextNode(text) {\n    return new TextNode(text);\n  },\n  createDocumentFragment() {\n    return new FragmentNode();\n  },\n  __setRoot(root) {\n    documentRoot = root;\n  },\n  querySelector(selector) {\n    if (!documentRoot) throw new Error(\"domjs: document.querySelector is not supported until a Surface Document template root is installed. Hold references to nodes you created.\");\n    return queryWithin(documentRoot, selector, true);\n  },\n  querySelectorAll(selector) {\n    if (!documentRoot) throw new Error(\"domjs: document.querySelectorAll is not supported until a Surface Document template root is installed. Hold references to nodes you created.\");\n    return queryAllWithin(documentRoot, selector, true);\n  },\n  getElementById(id) {\n    if (!documentRoot) {\n      throw new Error(\"domjs: document.getElementById is not supported until a Surface Document template root is installed. Hold references to nodes you created.\");\n    }\n    return walk(documentRoot, function(node) {\n      return node && node.__kind === \"element\" && node.id === String(id) ? node : null;\n    });\n  }\n};\nfunction region(renderFn) {\n  if (typeof renderFn !== \"function\") throw new Error(\"domjs: region(fn) requires a function.\");\n  return new RegionNode(renderFn);\n}\nfunction state(initial) {\n  return reactive(Object.assign({}, initial || {}));\n}\nexport {\n  ElementNode,\n  FragmentNode,\n  RegionNode,\n  TextNode,\n  document,\n  reactive,\n  region,\n  state\n};\n";
